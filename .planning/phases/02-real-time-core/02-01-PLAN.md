---
phase: 02-real-time-core
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/constants.ts
  - tests/api/vote.test.ts
  - tests/api/story.test.ts
  - tests/api/reveal.test.ts
autonomous: true
requirements:
  - VOTE-01
  - VOTE-02
  - VOTE-05
  - SESS-04
  - VOTE-04

must_haves:
  truths:
    - "FIBONACCI_DECK constant is exported from lib/constants.ts and contains exactly the 9 values: 1 2 3 5 8 13 21 ∞ ?"
    - "tests/api/vote.test.ts exists with failing (skipped) stubs for VOTE-01, VOTE-02, VOTE-05"
    - "tests/api/story.test.ts exists with failing (skipped) stubs for SESS-04"
    - "tests/api/reveal.test.ts exists with failing (skipped) stubs for VOTE-04"
    - "Running npx vitest run exits without crashing (stubs use describe.skip so suite passes)"
  artifacts:
    - path: "lib/constants.ts"
      provides: "FIBONACCI_DECK tuple and FibonacciValue type"
      exports:
        - FIBONACCI_DECK
        - FibonacciValue
    - path: "tests/api/vote.test.ts"
      provides: "Test stubs for vote API"
      contains: "describe.skip"
    - path: "tests/api/story.test.ts"
      provides: "Test stubs for story API"
      contains: "describe.skip"
    - path: "tests/api/reveal.test.ts"
      provides: "Test stubs for reveal API"
      contains: "describe.skip"
  key_links:
    - from: "lib/constants.ts"
      to: "app/api/rooms/[roomId]/vote/route.ts"
      via: "Zod z.enum(FIBONACCI_DECK) validation"
      pattern: "FIBONACCI_DECK"
    - from: "lib/constants.ts"
      to: "components/CardDeck.tsx"
      via: "import { FIBONACCI_DECK } from '@/lib/constants'"
      pattern: "FIBONACCI_DECK"
---

<objective>
Create the Wave 0 contract file and test stubs that downstream plans will implement against.

Purpose: Nyquist compliance — every subsequent task must have an automated verify target. The FIBONACCI_DECK constant is shared between server-side Zod validation and the client-side CardDeck component; defining it once prevents drift.
Output: lib/constants.ts with FIBONACCI_DECK export; three failing test stub files covering all API behaviors required by VOTE-01, VOTE-02, VOTE-04, VOTE-05, SESS-04.
</objective>

<execution_context>
@/Users/reinisgraudins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reinisgraudins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-real-time-core/02-RESEARCH.md
@.planning/phases/02-real-time-core/02-VALIDATION.md

<interfaces>
<!-- Key types available for test authoring. -->

From types/room.ts:
```typescript
export type RoomData = {
  roomId: string
  hostToken: string        // SHA-256 hash stored in Redis
  revealed: boolean
  voteCount: number
  currentStory: string
  createdAt: number
}

export type ParticipantData = {
  participantId: string
  name: string
  role: 'voter' | 'observer'
  joinedAt: string
}
```

From tests/api/join.test.ts (mock patterns to follow):
```typescript
// vi.mock pattern for mocking lib/room
vi.mock('@/lib/room', () => ({
  getRoom: vi.fn(),
  joinRoom: vi.fn().mockResolvedValue(undefined),
  keys: { room: (id: string) => `room:${id}`, votes: (id: string) => `room:${id}:votes` },
}))

// vi.mock pattern for mocking next/headers cookies
const mockSet = vi.fn()
const mockGet = vi.fn().mockReturnValue(undefined)
vi.mock('next/headers', () => ({
  cookies: vi.fn().mockResolvedValue({ set: mockSet, get: mockGet }),
}))

// params pattern for Next.js 16 route handlers
const params = Promise.resolve({ roomId: 'test-room' })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/constants.ts with FIBONACCI_DECK</name>
  <files>lib/constants.ts</files>
  <action>
Create lib/constants.ts with a single exported constant and type:

```typescript
// lib/constants.ts
// Fibonacci planning poker deck — single source of truth for server validation and client UI
export const FIBONACCI_DECK = ['1', '2', '3', '5', '8', '13', '21', '∞', '?'] as const
export type FibonacciValue = (typeof FIBONACCI_DECK)[number]
```

The `as const` assertion produces a readonly tuple type that Zod's `z.enum()` accepts directly. Both the vote API route (server-side Zod validation) and the CardDeck component (client-side rendering) will import this. Do NOT define this constant in any other file.
  </action>
  <verify>
    <automated>cd /Users/reinisgraudins/Documents/Projects/Planning_poker && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>lib/constants.ts exists, exports FIBONACCI_DECK and FibonacciValue, TypeScript compiles with no new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create failing test stubs for vote, story, and reveal APIs</name>
  <files>tests/api/vote.test.ts, tests/api/story.test.ts, tests/api/reveal.test.ts</files>
  <action>
Create three test stub files. Use `describe.skip` so the suite passes (no failing tests yet) but the structure and intent are declared. Each stub contains the exact behaviors Wave 1 will implement.

**tests/api/vote.test.ts** — covers VOTE-01, VOTE-02, VOTE-05:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

// Mocks will be filled in by Wave 1 implementation
vi.mock('@/lib/redis', () => ({
  redis: {
    hset: vi.fn().mockResolvedValue(1),
    hgetall: vi.fn().mockResolvedValue(null),
    multi: vi.fn(),
  },
}))
vi.mock('@/lib/room', () => ({
  getRoom: vi.fn(),
  keys: {
    room: (id: string) => `room:${id}`,
    votes: (id: string) => `room:${id}:votes`,
  },
}))
vi.mock('@/lib/pusher', () => ({
  pusherServer: { trigger: vi.fn().mockResolvedValue(undefined) },
  roomChannel: vi.fn((id: string) => `room-${id}`),
}))
vi.mock('next/headers', () => ({
  cookies: vi.fn().mockResolvedValue({
    get: vi.fn().mockReturnValue({ value: 'participant-uuid-1' }),
  }),
}))

describe.skip('POST /api/rooms/[roomId]/vote — VOTE-01', () => {
  it('stores valid Fibonacci value in votes hash and returns { ok: true }')
  it('rejects non-Fibonacci value with 400')
  it('rejects missing value with 400')
})

describe.skip('POST /api/rooms/[roomId]/vote — VOTE-02', () => {
  it('does not return vote value in response body (value stays server-side)')
})

describe.skip('POST /api/rooms/[roomId]/vote — VOTE-05', () => {
  it('returns 409 when room.revealed is true')
  it('returns 401 when participant cookie is absent')
})
```

**tests/api/story.test.ts** — covers SESS-04:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

vi.mock('@/lib/redis', () => ({
  redis: {
    hset: vi.fn().mockResolvedValue(1),
    hgetall: vi.fn().mockResolvedValue(null),
  },
}))
vi.mock('@/lib/room', () => ({
  getRoom: vi.fn(),
  keys: { room: (id: string) => `room:${id}` },
}))
vi.mock('@/lib/pusher', () => ({
  pusherServer: { trigger: vi.fn().mockResolvedValue(undefined) },
  roomChannel: vi.fn((id: string) => `room-${id}`),
}))
vi.mock('@/lib/auth', () => ({
  validateToken: vi.fn().mockResolvedValue(true),
}))
vi.mock('next/headers', () => ({
  cookies: vi.fn().mockResolvedValue({
    get: vi.fn().mockReturnValue({ value: 'raw-host-token' }),
  }),
}))

describe.skip('POST /api/rooms/[roomId]/story — SESS-04', () => {
  it('sets currentStory in Redis and triggers story-updated Pusher event')
  it('returns 403 when host-token cookie is absent')
  it('returns 403 when host token does not validate against stored hash')
  it('returns 400 when title is empty string')
  it('returns 400 when title exceeds 200 characters')
})
```

**tests/api/reveal.test.ts** — covers VOTE-04:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

const mockExec = vi.fn().mockResolvedValue([1, {}])
const mockHset = vi.fn()
const mockHgetall = vi.fn()
vi.mock('@/lib/redis', () => ({
  redis: {
    hset: mockHset,
    hgetall: mockHgetall,
    multi: vi.fn(() => ({ hset: mockHset, hgetall: mockHgetall, exec: mockExec })),
  },
}))
vi.mock('@/lib/room', () => ({
  getRoom: vi.fn(),
  keys: {
    room: (id: string) => `room:${id}`,
    votes: (id: string) => `room:${id}:votes`,
  },
}))
vi.mock('@/lib/pusher', () => ({
  pusherServer: { trigger: vi.fn().mockResolvedValue(undefined) },
  roomChannel: vi.fn((id: string) => `room-${id}`),
}))
vi.mock('@/lib/auth', () => ({
  validateToken: vi.fn().mockResolvedValue(true),
}))
vi.mock('next/headers', () => ({
  cookies: vi.fn().mockResolvedValue({
    get: vi.fn().mockReturnValue({ value: 'raw-host-token' }),
  }),
}))

describe.skip('POST /api/rooms/[roomId]/reveal — VOTE-04', () => {
  it('sets revealed=true in Redis using atomic multi() transaction')
  it('triggers vote-revealed Pusher event with votes map in payload')
  it('returns 403 when host-token cookie is absent')
  it('returns 403 when host token does not validate')
  it('returns 409 when room is already revealed')
  it('returns 404 when room does not exist')
})
```

The stub files must NOT import the route handlers yet (routes do not exist). The `describe.skip` wrapper means Vitest discovers and reports these as skipped, not failing.
  </action>
  <verify>
    <automated>cd /Users/reinisgraudins/Documents/Projects/Planning_poker && npx vitest run tests/api/vote.test.ts tests/api/story.test.ts tests/api/reveal.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All three test files exist. Vitest run reports them as skipped (0 failing). Suite exits with code 0.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no new TypeScript errors
2. `npx vitest run` — full suite exits 0 (skipped stubs do not count as failures)
3. `lib/constants.ts` exports `FIBONACCI_DECK` array of exactly 9 string values
</verification>

<success_criteria>
- lib/constants.ts created with FIBONACCI_DECK and FibonacciValue export
- Three test stub files created (vote, story, reveal) using describe.skip
- Full Vitest suite passes (0 failures, some skipped)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-real-time-core/02-01-SUMMARY.md`
</output>
