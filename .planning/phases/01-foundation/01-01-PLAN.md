---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - tsconfig.json
  - tests/lib/room.test.ts
  - tests/lib/auth.test.ts
  - tests/api/rooms.test.ts
  - tests/api/join.test.ts
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - IDNT-01
  - IDNT-02

must_haves:
  truths:
    - "Next.js 16 project exists with TypeScript and Tailwind"
    - "All required Phase 1 packages are installed"
    - "Vitest is configured with path aliases matching tsconfig"
    - "Test stub files exist and Vitest reports them as failing (RED state)"
    - "No production code exists yet — stubs only"
  artifacts:
    - path: "package.json"
      provides: "All Phase 1 dependencies installed"
      contains: "@upstash/redis"
    - path: "vitest.config.ts"
      provides: "Vitest configuration with @/ alias"
    - path: "tests/lib/room.test.ts"
      provides: "Failing stubs for SESS-01, SESS-03, IDNT-02"
    - path: "tests/lib/auth.test.ts"
      provides: "Failing stubs for SESS-02 token roundtrip"
    - path: "tests/api/rooms.test.ts"
      provides: "Failing stubs for SESS-01, SESS-02 route integration"
    - path: "tests/api/join.test.ts"
      provides: "Failing stubs for IDNT-01, IDNT-02 join route"
  key_links:
    - from: "vitest.config.ts"
      to: "tsconfig.json"
      via: "path alias @/ → src/"
      pattern: "alias.*@/"
---

<objective>
Scaffold the Next.js project and establish Wave 0 test infrastructure.

Purpose: All Phase 1 work depends on (a) a runnable Next.js project and (b) test stubs that define the expected behaviors before implementation. Without this plan, later plans have no project to modify and no automated verification to run against.

Output: A scaffolded Next.js 16 project with all Phase 1 packages installed, a Vitest config, and four test stub files in RED state.
</objective>

<execution_context>
@/Users/reinisgraudins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reinisgraudins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project and install Phase 1 dependencies</name>
  <files>package.json, tsconfig.json, next.config.ts, tailwind.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <action>
Run `create-next-app` in the project root (which is currently an empty git repo):

```bash
npx create-next-app@latest . --typescript --tailwind --app --no-src-dir --import-alias "@/*" --yes
```

NOTE: Because the directory already contains `.git` and `.planning/`, use `.` as the destination and accept the "directory not empty" prompt. The `--yes` flag handles this.

After scaffolding, install Phase 1 runtime and dev dependencies:

```bash
npm install pusher pusher-js @upstash/redis nanoid zod
npm install -D vitest @vitest/coverage-v8
```

Verify the installed versions by checking `package.json`. The expected packages:
- `@upstash/redis` ^1.37.0
- `pusher` ^5.3.3
- `pusher-js` ^8.4.2
- `nanoid` ^5.1.7
- `zod` ^4.3.6
- `vitest` (latest)
- `@vitest/coverage-v8` (latest)

Do NOT install `cookies-next` — that is a Phase 2 client component concern. Do NOT install uuid, bcrypt, or jsonwebtoken — not needed per research.
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); const deps = {...p.dependencies, ...p.devDependencies}; ['@upstash/redis','pusher','pusher-js','nanoid','zod','vitest'].forEach(d => { if (!deps[d]) throw new Error('Missing: ' + d) }); console.log('All deps present')"</automated>
  </verify>
  <done>package.json contains all Phase 1 dependencies, `npm run dev` would start the dev server, `tsconfig.json` has `@/*` path alias pointing to `./*`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Vitest config and test stub files</name>
  <files>vitest.config.ts, tests/lib/room.test.ts, tests/lib/auth.test.ts, tests/api/rooms.test.ts, tests/api/join.test.ts</files>
  <behavior>
    - room.test.ts stub: "createRoom" test fails (no src/lib/room.ts yet)
    - room.test.ts stub: "redis schema" test fails
    - room.test.ts stub: "TTL" test fails
    - room.test.ts stub: "participantId" test fails
    - room.test.ts stub: "toRoomView redacted" test — when revealed=false, vote values MUST NOT appear in output
    - auth.test.ts stub: "token roundtrip" test fails (no src/lib/auth.ts yet)
    - rooms.test.ts stub: "host token cookie" test fails (no route handler yet)
    - join.test.ts stub: "join" test fails (no join route yet)
  </behavior>
  <action>
Create `vitest.config.ts` at the project root:

```typescript
import { defineConfig } from 'vitest/config'
import path from 'path'

export default defineConfig({
  test: {
    environment: 'node',
    globals: true,
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, '.'),
    },
  },
})
```

NOTE: The `@/*` alias in `tsconfig.json` from `create-next-app` maps to `./*` (project root). Mirror this exactly in vitest.config.ts. Do NOT point it at `./src` — Next.js App Router with `--no-src-dir` places files at root level (e.g., `app/`, `lib/`, `types/`).

Create `tests/lib/room.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

// These imports will fail until lib/room.ts exists — that is intentional (RED state)
// Uncomment as implementation is added in Plan 02
// import { createRoom, joinRoom, toRoomView, keys } from '@/lib/room'

describe('createRoom', () => {
  it('returns void and creates Redis hash with correct fields', async () => {
    // STUB: will fail until lib/room.ts exports createRoom
    expect(true).toBe(false) // Force RED
  })

  it('redis schema — hash contains hostToken, revealed, voteCount, currentStory, createdAt', async () => {
    expect(true).toBe(false) // Force RED
  })
})

describe('TTL', () => {
  it('sets 86400-second TTL on room key after createRoom', async () => {
    expect(true).toBe(false) // Force RED
  })
})

describe('participantId', () => {
  it('joinRoom stores participantId as hash key, not display name', async () => {
    expect(true).toBe(false) // Force RED
  })
})

describe('toRoomView redacted', () => {
  it('does not include vote values when revealed=false', async () => {
    expect(true).toBe(false) // Force RED
  })

  it('includes vote values when revealed=true', async () => {
    expect(true).toBe(false) // Force RED
  })
})
```

Create `tests/lib/auth.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'

// import { hashToken, validateToken } from '@/lib/auth'

describe('token roundtrip', () => {
  it('hashToken produces a deterministic SHA-256 hex string', async () => {
    expect(true).toBe(false) // Force RED
  })

  it('validateToken returns true when cookie value matches stored hash', async () => {
    expect(true).toBe(false) // Force RED
  })

  it('validateToken returns false when cookie value does not match', async () => {
    expect(true).toBe(false) // Force RED
  })
})
```

Create `tests/api/rooms.test.ts`:

```typescript
import { describe, it, expect, vi } from 'vitest'

// Route handler integration test — mocks Redis and Pusher
// import { POST } from '@/app/api/rooms/route'

describe('host token cookie', () => {
  it('POST /api/rooms returns 200 with roomId and shareUrl', async () => {
    expect(true).toBe(false) // Force RED
  })

  it('POST /api/rooms sets httpOnly host-token-{roomId} cookie', async () => {
    expect(true).toBe(false) // Force RED
  })
})
```

Create `tests/api/join.test.ts`:

```typescript
import { describe, it, expect, vi } from 'vitest'

// import { POST } from '@/app/api/rooms/[roomId]/join/route'

describe('join', () => {
  it('POST join with { name } returns 200 with participantId', async () => {
    expect(true).toBe(false) // Force RED
  })

  it('POST join sets httpOnly participant-{roomId} cookie', async () => {
    expect(true).toBe(false) // Force RED
  })

  it('POST join stores UUID as key in participants hash, not display name', async () => {
    expect(true).toBe(false) // Force RED
  })

  it('POST join with existing participant cookie returns existing participantId without creating new one', async () => {
    expect(true).toBe(false) // Force RED
  })
})
```

Run `npx vitest run` and confirm ALL tests fail (RED state). This is the expected and correct outcome for Wave 0.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>Vitest runs, all test files are found, all tests fail with "expected true to be false" (or similar). Zero passing tests, zero skipped. No import/config errors — failures are intentional assertion failures, not module resolution errors.</done>
</task>

</tasks>

<verification>
Run the full suite and confirm the expected RED state:
```
npx vitest run
```
Expected: 0 passed, N failed. Failure reason must be assertion failures, NOT "Cannot find module" errors (those would indicate vitest.config.ts alias misconfiguration).

Check that `@/` alias resolves correctly by verifying vitest.config.ts alias matches tsconfig.json `paths`.
</verification>

<success_criteria>
1. `ls package.json next.config.ts tsconfig.json vitest.config.ts` — all exist
2. `node -e "require('./package.json').dependencies['@upstash/redis']"` — prints a version string
3. `npx vitest run` exits with non-zero (tests fail) but no module resolution errors
4. Four test stub files exist under `tests/lib/` and `tests/api/`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template.
</output>
