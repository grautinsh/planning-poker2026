---
phase: 03-session-output
plan: 06
type: execute
wave: 3
depends_on: [03-04, 03-05]
files_modified: []
autonomous: false
requirements: [POST-01, POST-02, POST-03, LOG-01, LOG-02, LOG-03]

must_haves:
  truths:
    - "Full test suite passes green before checkpoint begins"
    - "After reveal, voter names + card values visible (POST-01)"
    - "Min/max/avg stats row (or green consensus badge) visible above VoteCard grid (POST-02)"
    - "Reset Round clears votes and returns to voting phase on all connected clients (POST-03)"
    - "Next Story inline estimate entry appears in HostControls panel (no modal) (LOG-01)"
    - "Session log at page bottom accumulates completed stories in real time across all clients (LOG-02)"
    - "Copy button produces correct markdown table, label briefly shows checked state (LOG-03)"
  artifacts:
    - path: "app/room/[roomId]/page.tsx"
      provides: "Fully wired room page with all Phase 3 features"
    - path: "components/HostControls.tsx"
      provides: "Three-state component verified working in browser"
    - path: "components/SessionLog.tsx"
      provides: "Session log component verified working in browser"
  key_links:
    - from: "Browser clipboard"
      to: "SessionLog copy button"
      via: "navigator.clipboard.writeText in handleCopy"
      pattern: "verified by pasting into text editor"
    - from: "Pusher story-logged event"
      to: "SessionLog re-render on all clients"
      via: "useRoom binding → refreshRoom → new log[] in GET response"
      pattern: "verified by two-tab test"
---

<objective>
Run the full test suite and perform human verification of all Phase 3 features in a live browser. This is the quality gate before Phase 3 is marked complete.

Purpose: Confirm all requirements are observable from the user's perspective. Automated tests verify logic; this checkpoint verifies the integrated experience.
Output: Phase 3 sign-off.
</objective>

<execution_context>
@/Users/reinisgraudins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reinisgraudins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-session-output/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite gate</name>
  <files></files>
  <action>
    Run the full Vitest suite. All tests must pass before the human checkpoint proceeds.

    If any test fails:
    1. Identify which file and test case
    2. Check which plan created that implementation (Plans 02, 03, 04, or 05)
    3. Fix the implementation in the relevant file
    4. Re-run until green

    Do not proceed to Task 2 until full suite is green.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>npx vitest run exits 0. All test files pass. No test is marked as failing or skipped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 3 features:
    - POST-01: Voter name + card value visible after reveal (VoteCard grid was already built in Phase 2; no code change needed, verifying it still works)
    - POST-02: Vote stats row above VoteCard grid (min/max/avg or green consensus badge)
    - POST-03: Reset Round button in HostControls post-reveal, clears votes and returns to voting phase
    - LOG-01: Next Story inline estimate entry in HostControls panel (no modal)
    - LOG-02: Session log at page bottom, real-time updates across all clients
    - LOG-03: Copy button produces markdown table, label briefly changes to checked state
  </what-built>
  <how-to-verify>
    Run the dev server: `npm run dev`
    Open http://localhost:3000 in two browser tabs (or windows).

    **Setup:**
    1. Tab A: Create a room → host joins automatically
    2. Tab B: Open the share URL → join as a voter (name: "Alice")
    3. (Optional) Tab C: Join as a second voter (name: "Bob")

    **POST-01 + POST-02 — Vote and reveal:**
    4. Host (Tab A): Enter story title "Login form"
    5. Tab B (Alice): Vote by selecting a card value (e.g., 5)
    6. If Tab C exists: Vote with a different value (e.g., 8)
    7. Host (Tab A): Click "Reveal Votes"
    8. VERIFY: All voter cards are face-up showing names + values on all tabs
    9. VERIFY: Stats row appears above cards. If 2 voters with different values: "Min: 5 · Max: 8 · Avg: 6.5". If single voter: "✓ Consensus: 5" in green.

    **POST-03 — Reset round:**
    10. Host (Tab A): Click "Reset Round"
    11. VERIFY on Tab A: VoteCard grid disappears, CardDeck reappears for voters, stats row gone
    12. VERIFY on Tab B: Same — VoteCard grid gone, CardDeck back, story title "Login form" still visible

    **LOG-01 — Next story estimate entry:**
    13. Host (Tab A): Vote (or have Tab B vote), then Reveal
    14. Host (Tab A): Click "Next Story →"
    15. VERIFY on Tab A: Inline estimate input + Confirm + Cancel appear inside the HostControls panel (no modal, no new page)
    16. Type "5" in estimate input, click Cancel
    17. VERIFY: Returns to Reset Round / Next Story button state
    18. Click "Next Story →" again, type "5", click Confirm

    **LOG-02 — Session log real-time:**
    19. VERIFY on Tab A: "Login form · 5" entry appears in session log at page bottom immediately
    20. VERIFY on Tab B: Same entry appears without page refresh
    21. VERIFY: HostControls returns to voting state, story title cleared
    22. Repeat with a second story: set title "API integration", vote, reveal, Next Story → estimate "8"
    23. VERIFY on all tabs: Both entries in session log ("Login form · 5" and "API integration · 8")

    **LOG-03 — Clipboard export:**
    24. Click the "Copy" button in the session log header
    25. VERIFY: Button label briefly shows "✓ Copied!" for ~2 seconds then reverts to "Copy"
    26. Paste into any text editor
    27. VERIFY pasted content is:
        ```
        | Story | Points |
        |-------|--------|
        | Login form | 5 |
        | API integration | 8 |
        ```
        (no extra lines, no date header, no total)

    **Edge cases to spot-check:**
    - Empty log: Copy button should be disabled (greyed out) before any stories are logged
    - "No stories logged yet" empty state visible before first Next Story action
  </how-to-verify>
  <resume-signal>Type "approved" if all behaviors verified, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
npx vitest run 2>&1 | tail -5
</verification>

<success_criteria>
- npx vitest run exits 0 (full suite green)
- Human verification: all 6 requirements (POST-01 through LOG-03) confirmed working in browser
- Real-time updates verified across two browser tabs
- Clipboard export produces correct markdown table format
- Phase 3 approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-output/03-06-SUMMARY.md`
</output>
